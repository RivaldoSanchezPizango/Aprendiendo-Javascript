let indefinida // undefined -- No tiene valor y tampoco se asigno uno cuando se creo
console.log(indefinida);

let nulo = null // null -- variable vacia que deja el programador intencionalmente
console.log(null);

let noEsUnNumero = "hola" * 3.7; // NaN -- no es un numero
console.log(noEsUnNumero);